.\" "Generated by swift-argument-parser"
.Dd July 19, 2026
.Dt VAPORIZE.CLI@WRKSTRM-CORE.CLIA.SH 1
.Os
.Sh NAME
.Nm vaporize.cli@wrkstrm-core.clia.sh
.Nd "Substrate-canonical vaporware-collapse gate. vaporize transmutes typed substrate-vaporware into world-state: binaries land, .app installs, processes run, receipts emit. Modes: install, uninstall, build, test, run, pass, use, toolchain-selection, setup. Toolchain selection compiles swiftlang/swiftly's `use` operation into this executable for default Swift selection. On macOS, the independent `toolchain-selection xcode` provider compiles in the xcode-select selection surface. Vaporize does not locate or launch an installed swiftly CLI. Toolchain lifecycle, general inspection, and execution remain separate responsibilities. Vaporware-awareness modes: status + warehouse enumerate and store vaporware at a path per the `x-vaporize-collapse-path` annotation convention. Project-generation bridge mode: inspect-project-yml reads legacy XcodeGen YAML into an owned Swift model without rewriting it; compare-project-yml-pkl compares that model with a Pkl parity specimen; import-project-yml emits a Pkl parity specimen from legacy YAML; generate-project-yml emits transitional AppleProjectSpec YAML from Pkl with receipts; generate-xcodeproj emits first-slice .xcodeproj world-state from evaluated AppleProjectSpec Pkl; generate-sparkle-config emits a compiled-in SparkleConfig.swift for a CLI tool target from its project.pkl releaseIdentity; list-targets discovers project targets, packages, and schemes for parity/build routing; list-schemes asks xcodebuild for live .xcworkspace schemes; release-doctor audits the release spine before claims are trusted. fleet-status reports every installed bin-directory tool with its sidecar-recorded version and appcast feed currency (current/behind/critical-behind/feed-unreachable/no-sidecar). `inventory` discovers Package.swift, .xcodeproj, .xcworkspace, project.yml, and project.pkl surfaces by domain, product line, and ownership scope. `cuj-audit` inventories CUJ definitions, proof bindings, canonical product homes, and active-owned implementation coverage without conflating fixtures, matrices, receipts, or tests; `--proof-ledger-path` writes the canonical cross-portfolio proof index while `--project-ledger-path` and `--project-ledger-csv-path` write one fine-grained coverage row per active-owned implementation project. Executable tests and green receipts remain in their owning homes. `domains` lists available tool domains from the tools collection. Legacy `x-craze-collapse-path` remains a compatibility alias during migration. Target feature inspection mode: inspect-target-features reads a project.yml target, its configFiles wiring, release-features.json, generated xcconfigs, and ReleaseFeatures.swift provenance."
.Sh SYNOPSIS
.Nm
.Op Ar mode
.Op Fl -version
.Op Fl -artifact Ar artifact
.Op Fl -package-path Ar package-path
.Op Fl -product Ar product
.Op Fl -product-version Ar product-version
.Op Fl -product-build Ar product-build
.Op Fl -product-build-sha Ar product-build-sha
.Op Fl -product-build-date Ar product-build-date
.Op Fl -su-feed-url Ar su-feed-url
.Op Fl -su-public-ed-key Ar su-public-ed-key
.Op Fl -app-bundle-name Ar app-bundle-name
.Op Fl -configuration Ar configuration
.Op Fl -destination Ar destination
.Op Fl -force
.Op Fl -skip-build
.Op Fl -skip-install
.Op Fl -launch
.Op Fl -xcode-project Ar xcode-project
.Op Fl -xcode-workspace Ar xcode-workspace
.Op Fl -scheme Ar scheme
.Op Fl -target Ar target
.Op Fl -derived-data-path Ar derived-data-path
.Op Fl -xcode-product-cache-workspace Ar xcode-product-cache-workspace
.Op Fl -xcode-product-cache-derived-data-path Ar xcode-product-cache-derived-data-path
.Op Fl -xcode-destination Ar xcode-destination...
.Op Fl -xcode-sdk Ar xcode-sdk
.Op Fl -xcode-result-bundle-path Ar xcode-result-bundle-path
.Op Fl -xcode-build-setting Ar xcode-build-setting...
.Op Fl -analyze
.Op Fl -receipt-path Ar receipt-path
.Op Fl -report-path Ar report-path
.Op Fl -proof-ledger-path Ar proof-ledger-path
.Op Fl -project-ledger-path Ar project-ledger-path
.Op Fl -project-ledger-csv-path Ar project-ledger-csv-path
.Op Fl -working-directory Ar working-directory
.Op Fl -common-process-spec Ar common-process-spec
.Op Fl -developer-dir Ar developer-dir
.Op Fl -swift-source Ar swift-source
.Op Fl -xcode-component Ar xcode-component
.Op Fl -path Ar path
.Op Fl -schema Ar schema
.Op Fl -fixture Ar fixture
.Op Fl -expect Ar expect
.Op Fl -pkl-path Ar pkl-path
.Op Fl -pkl-schema-path Ar pkl-schema-path
.Op Fl -output-path Ar output-path
.Op Fl -output Ar output
.Op Fl -apply
.Op Fl -format Ar format
.Op Fl -bin-dir Ar bin-dir
.Op Fl -domain Ar domain
.Op Fl -tools-collection Ar tools-collection
.Op Ar forwarded-arguments...
.Op Fl -help
.Sh DESCRIPTION
Toolchain selection structure:
  vaporize toolchain-selection swift -- use [swiftly-use-options] [selector]
  vaporize toolchain-selection xcode -- select <xcode-select-options>
Swift and Xcode are independent selection domains. Selection does not own
toolchain lifecycle, general inspection, or execution.
.Bl -tag -width 6n
.It Ar mode
Mode: install, uninstall, build, test, run, pass, use, toolchain-selection, setup, status, warehouse, validate-json, validate-json-schema, inspect-project-yml, inspect-target-features, compare-project-yml-pkl, import-project-yml, upgrade-project-yml-to-pkl, generate-project-yml, generate-xcodeproj, generate-sparkle-config, list-targets, list-schemes, release-doctor, inventory, cuj-audit, domains, self-update, fleet-status, graph (forwards to package-graph@wrkstrm.cli), or the deprecated cli and app compatibility spellings.
.It Fl -version
Prints the tool name, version, and build metadata and exits.
.It Fl -artifact Ar artifact
Artifact kind: cli or app.
.It Fl -package-path Ar package-path
Path to the Swift package.
.It Fl -product Ar product
Product name (binary or app bundle).
.It Fl -product-version Ar product-version
Version recorded as CFBundleShortVersionString in the installed CLI metadata Info.plist.
.It Fl -product-build Ar product-build
Build recorded as CFBundleVersion in the installed CLI metadata Info.plist.
.It Fl -product-build-sha Ar product-build-sha
Source revision recorded in the installed CLI metadata Info.plist.
.It Fl -product-build-date Ar product-build-date
Build date recorded in the installed CLI metadata Info.plist.
.It Fl -su-feed-url Ar su-feed-url
Sparkle appcast URL recorded as SUFeedURL in the installed CLI metadata Info.plist sidecar (requires --su-public-ed-key).
.It Fl -su-public-ed-key Ar su-public-ed-key
Base64 ed25519 public key recorded as SUPublicEDKey in the installed CLI metadata Info.plist sidecar (requires --su-feed-url).
.It Fl -app-bundle-name Ar app-bundle-name
Built .app bundle name when it differs from --product (app mode).
.It Fl -configuration Ar configuration
Build configuration.
.Pp
.It Fl -destination Ar destination
Destination directory for app install (default /Applications).
.It Fl -force
Replace existing install.
.It Fl -skip-build
Skip build (app mode only).
.It Fl -skip-install
Skip the default install step for build or run mode.
.It Fl -launch
Launch app after install (app mode).
.It Fl -xcode-project Ar xcode-project
Path to .xcodeproj when building with xcodebuild (app mode).
.It Fl -xcode-workspace Ar xcode-workspace
Path to .xcworkspace when building with xcodebuild (app mode) or listing workspace schemes.
.It Fl -scheme Ar scheme
Scheme to build with xcodebuild (requires --xcode-project or --xcode-workspace).
.It Fl -target Ar target
Target name for inspect-target-features or generate-sparkle-config mode.
.It Fl -derived-data-path Ar derived-data-path
Derived data path to use with xcodebuild; also searched for the built .app bundle.
.It Fl -xcode-product-cache-workspace Ar xcode-product-cache-workspace
Shared .xcworkspace whose warm product cache should be queried before per-project app build outputs.
.It Fl -xcode-product-cache-derived-data-path Ar xcode-product-cache-derived-data-path
DerivedData root for --xcode-product-cache-workspace; Vaporize searches Build/Products/<configuration>/<app>.app there first.
.It Fl -xcode-destination Ar xcode-destination...
Typed xcodebuild destination. Repeat for multiple destinations. Defaults to macOS arm64 when using xcodebuild.
.It Fl -xcode-sdk Ar xcode-sdk
SDK passed to xcodebuild with -sdk.
.It Fl -xcode-result-bundle-path Ar xcode-result-bundle-path
Result bundle path passed to xcodebuild with -resultBundlePath.
.It Fl -xcode-build-setting Ar xcode-build-setting...
Build setting passed to xcodebuild as KEY=VALUE. Repeat for multiple settings.
.It Fl -analyze
Emit a JSON receipt for test, pass, use, or toolchain-selection mode.
.It Fl -receipt-path Ar receipt-path
Write the test, pass-through, use, toolchain-selection, inventory, or CUJ audit JSON receipt to this path.
.It Fl -report-path Ar report-path
Write the CUJ audit Markdown report to this path.
.It Fl -proof-ledger-path Ar proof-ledger-path
Write the canonical CUJ automated-proof ledger to this path.
.It Fl -project-ledger-path Ar project-ledger-path
Write the fine-grained active-owned implementation project CUJ coverage ledger to this path.
.It Fl -project-ledger-csv-path Ar project-ledger-csv-path
Write the fine-grained implementation project CUJ coverage ledger as CSV to this path.
.It Fl -working-directory Ar working-directory
Working directory for pass mode. Defaults to the current directory.
.It Fl -common-process-spec Ar common-process-spec
Path to a CommonProcess CommandSpec JSON for use mode. Use '-' for stdin.
.It Fl -developer-dir Ar developer-dir
DEVELOPER_DIR override for SwiftPM CLI build/install/test with --swift-source xcode; it does not change the xcode-select selection.
.It Fl -swift-source Ar swift-source
Swift executable source for SwiftPM CLI build/install/test: default uses the selected Swift on PATH; xcode is available only on macOS and uses xcrun without changing the default Swift selection.
.Pp
.It Fl -xcode-component Ar xcode-component
Xcode component to download in setup mode, for example MetalToolchain.
.It Fl -path Ar path
Path for status, warehouse, inventory, cuj-audit, validate-json, inspect-project-yml, inspect-target-features, compare-project-yml-pkl, import-project-yml, or list-targets modes.
.It Fl -schema Ar schema
JSON Schema file path for validate-json-schema mode.
.It Fl -fixture Ar fixture
Fixture JSON instance path validated against --schema in validate-json-schema mode.
.It Fl -expect Ar expect
Expected validate-json-schema outcome: pass or fail. Exit is nonzero when the actual outcome differs.
.It Fl -pkl-path Ar pkl-path
Path to an AppleProjectSpec Pkl record for compare-project-yml-pkl, generate-project-yml, generate-xcodeproj, or list-targets mode.
.It Fl -pkl-schema-path Ar pkl-schema-path
Path to AppleProjectSpec.pkl for import-project-yml mode. Defaults to Vaporize's package schema when discoverable.
.It Fl -output-path Ar output-path
Output path for import-project-yml, generate-project-yml, or generate-xcodeproj mode.
.It Fl -output Ar output
Output path for the generated SparkleConfig.swift in generate-sparkle-config mode.
.It Fl -apply
For upgrade-project-yml-to-pkl mode: retire the project.yml after a parity-verified upgrade. Without it the upgrade is a dry-run preview.
.It Fl -format Ar format
Output format for status, inventory, cuj-audit, domains, inspect-project-yml, inspect-target-features, compare-project-yml-pkl, import-project-yml, generate-project-yml, generate-xcodeproj, list-targets, list-schemes, release-doctor, or fleet-status mode: text (default) or json.
.It Fl -bin-dir Ar bin-dir
Install bin directory scanned by fleet-status (default ~/.swiftpm/bin).
.It Fl -domain Ar domain
Tool domain for install/uninstall/run and domain path shaping (for example build).
.It Fl -tools-collection Ar tools-collection
Kura tools collection directory for domains mode.
.It Ar forwarded-arguments...
Arguments forwarded to test, run, pass, or toolchain-selection mode.
.It Fl h , -help
Show help information.
.It Em help
Show subcommand help information.
.Bl -tag -width 6n
.It Ar subcommands...
.El
.El
.Sh "EXIT STATUS"
.Ex -std
.Sh AUTHORS
The
.Nm
reference was written by
.An -nosplit
.An "Wrkstrm Core" .